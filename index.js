/**
 * Point d'entrée principal de l'application
 * Le serveur est démarré via server.js
 */

const server = require('./server');

// Export pour utilisation en module si nécessaire
module.exports = server;
